[package]
name = "hydration-conformance"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Executable specification for a Linux cloud-file hydration framework"

[dependencies]
libc = "0.2"

[dev-dependencies]
test-scratch.workspace = true
tempfile.workspace = true
