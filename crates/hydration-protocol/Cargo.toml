[package]
name = "hydration-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The wire format across the hydration privilege boundary"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
